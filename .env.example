# Local development derives the Worker URL from the page host on port 8788.
# Production builds use the deployed Cloudflare Worker.
VITE_MULTIPLAYER_API_URL=https://wispwise-multiplayer.wispwise-game.workers.dev
